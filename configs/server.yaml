listen_addr: ":9000"

tls:
    enabled: true
    cert_file: "certs/server.crt"
    key_file: "certs/server.key"

auth:
    token_ttl_minutes: 60

limits:
    max_connections: 100
    max_tunnel_duration_minutes: 60
